# CivicSolve AI — frontend environment
# Vite naming (for a standalone React SPA build)
VITE_API_BASE_URL=http://localhost:8080/api

# Next.js naming (used by the runnable app in this repository)
NEXT_PUBLIC_API_BASE_URL=/api
